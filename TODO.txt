To-Do List:

- Slimes are invis by default, when player collides with one it will become visible, change this so that
when player collides with one, it will become a fight instead of simply becoming visible
- Add visible inventory (with grid layout)
